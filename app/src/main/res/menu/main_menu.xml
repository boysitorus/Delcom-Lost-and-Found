<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:menuCategory="container">
        <item
            android:id="@+id/mainMenuProfile"
            android:title="Profile"
            app:showAsAction="never" />

        <item android:id="@+id/mainMenuAllReport"
            android:title="All Report"
            app:showAsAction="never"/>

        <item android:id="@+id/mainMenuMyReport"
            android:title="My Report"
            app:showAsAction="never"/>

        <item android:id="@+id/mainMenuMyFavorite"
            android:title="Favorite"
            app:showAsAction="never"/>

        <item
            android:id="@+id/mainMenuLogout"
            android:title="Logout"
            app:showAsAction="never" />
    </group>
</menu>